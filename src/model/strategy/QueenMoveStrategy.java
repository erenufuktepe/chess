package model.strategy;

import model.Board;
import model.Move;
import model.Piece;
import model.Position;

import java.util.List;

public class QueenMoveStrategy extends BaseMoveStrategy {
    @Override
    public List<Move> getLegalMoves(Position currentPosition, Board board, Piece piece) {
        return null;
    }
}
